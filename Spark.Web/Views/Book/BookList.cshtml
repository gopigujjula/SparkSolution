@using Sitecore.Mvc;
@using Spark.Web
@using Spark.Web.Models
@using Glass.Mapper.Sc.Web.Mvc
@model BookSearchResult

@if (Model != null && Model.Books != null && Model.Books.Any())
{
    <section class="recent-book-sec">
        <div class="container">
            <div class="title">
                <h2>Books</h2>
                <hr>
            </div>
            <div class="row">
                @foreach (var book in Model.Books)
                {
                    <div class="col-lg-3 col-md-6">
                        <div class="item">
                            @Html.Glass().RenderImage(book, f => f.BookCover, isEditable: true)
                            <h3>@Html.Glass().Editable(book, f => f.Title)</h3>
                            <h6>
                                @*<span class="price">$49</span> /*@
                                <a href="#">Buy Now</a> @*Dictionary Item*@
                            </h6>
                            <div class="hover">
                                <a href="@book.Url">
                                    <span>
                                        <i class="fa fa-long-arrow-right" aria-hidden="true">
                                        </i>
                                    </span>
                                </a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </section>
}
